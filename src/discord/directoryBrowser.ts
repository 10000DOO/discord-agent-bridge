// TODO(Phase 1): folder-picker sub-component used by the wizard (cwd selection), path-confined (§4, §9).
export class DirectoryBrowser {
  render(_startPath: string): unknown {
    throw new Error('not implemented');
  }
}
