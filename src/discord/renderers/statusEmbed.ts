// TODO(Phase 1): pinned session status — mode + permMode + usage-availability, cwd, session id,
// resumable. For Codex, a single "usage/limits unavailable (Codex CLI limitation)" line (§6, §7.4).
export function renderStatusEmbed(_status: unknown): void {
  throw new Error('not implemented');
}
