// TODO(Phase 1): in-process MCP attach_file tool, path-confined to the workspace root (§5a, §7.5).
export function createMcpFileTool(_workspaceRoot: string): unknown {
  throw new Error('not implemented');
}
